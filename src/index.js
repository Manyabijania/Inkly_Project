import React from "react";
import { createRoot } from "react-dom/client";
import Components from "./components/Appnew";
const root = createRoot(document.getElementById("root"));

root.render(<Components />);
